[tool.poetry]
name = "fastapi-elk"
version = "0.1.0"
description = ""
authors = ["Volney Casas"]
readme = "README.md"

[tool.poetry.dependencies]
python = "3.12.*"
fastapi = "^0.115.2"
uvicorn = "^0.31.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
